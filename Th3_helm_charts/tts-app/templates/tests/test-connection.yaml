apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tts-app.fullname" . }}-test-connection"
  labels:
    {{- include "tts-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tts-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
